---
phase: 01-design-polish
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/index.astro
autonomous: false
requirements:
  - DESIGN-02
must_haves:
  truths:
    - "User sees 'John Litzsinger' as a large hero name on the homepage, not in the top bar"
    - "Tagline 'Finance @ Purdue interested in startups.' appears as a subtitle below the name"
    - "jl logo remains in the top-left but is slightly larger (36px)"
    - "Folder grid remains below name+tagline, centered as a group in the remaining viewport space"
    - "Top bar no longer shows brand name text (only logo + social icons)"
  artifacts:
    - path: "src/pages/index.astro"
      provides: "Homepage with hero name layout"
      contains: "hero-name"
  key_links:
    - from: "src/pages/index.astro"
      to: "src/layouts/BaseLayout.astro"
      via: "BaseLayout scrollable=false (default), no breadcrumbs prop"
      pattern: "BaseLayout"
---

<objective>
Rework the homepage layout to move the brand name ("John Litzsinger") from the top bar into a hero position above the folder grid. The tagline becomes a subtitle beneath it. The jl logo stays top-left but slightly larger. Social icons remain top-right.

Purpose: The locked design decision establishes the name as the focal point of the homepage, not just navigation chrome. This creates the "name up top, single identity" feel referenced by walzr.com and pucek.capital.
Output: Updated index.astro with hero name + subtitle + folder grid composition. Updated BaseLayout brand-name visibility on homepage.
</objective>

<execution_context>
@/home/johnl/.claude/get-shit-done/workflows/execute-plan.md
@/home/johnl/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-design-polish/01-CONTEXT.md

<interfaces>
<!-- Key contracts from BaseLayout.astro that this plan consumes -->

From src/layouts/BaseLayout.astro:
- Props: title, description, scrollable?, breadcrumbs?
- When NO breadcrumbs passed: homepage mode — no section-header/breadcrumb rendered
- .brand-name in top-bar: currently always visible (hidden only at 480px via media query)
- --icon-filter defaults to 'none' when no section breadcrumb
- main element: flex:1, padding-top: var(--space-md), max-width 720px centered

From src/styles/global.css:
- --font-brand: 'Urbanist', system-ui, sans-serif  (Urbanist variable 100-900)
- --font: 'Rethink Sans' (body)
- --text-2xl: clamp(1.75rem, 1.46rem + 1.15vw, 2.5rem)
- --space-sm: 0.5rem, --space-md: 1rem, --space-lg: 2rem, --space-xl: 4rem
- --content-max: 720px
- --gutter: clamp(1rem, 0.59rem + 1.66vw, 2.5rem)

Current index.astro structure:
- .home: position relative, flex:1, --chrome-above: 5.25rem
- .tagline: position absolute, top: calc(33.33dvh - var(--chrome-above)), centered
- .folder-grid: position absolute, top: calc(50dvh - var(--chrome-above)), centered
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Homepage hero name layout</name>
  <files>src/pages/index.astro</files>
  <action>
Rework index.astro to implement the locked homepage design:

1. REMOVE the absolute-positioned tagline layout. Replace with a normal flow layout inside .home.

2. NEW STRUCTURE inside .home:
   - .home uses flex column, justify-content: center, align-items: center (centers name+folders as a group in the viewport)
   - .hero block: contains .hero-name (h1) + .hero-tagline (p), centered text
   - .folder-grid: below .hero with margin-top: var(--space-xl), normal flow (not absolute)

3. .hero-name STYLES:
   - font-family: var(--font-brand) (Urbanist)
   - font-size: clamp(2.5rem, 1.8rem + 2.8vw, 4rem) — large but not overwhelming
   - font-weight: 700
   - letter-spacing: -0.03em
   - color: var(--foreground)
   - line-height: 1.0

4. .hero-tagline STYLES:
   - font-family: var(--font) (Rethink Sans)
   - font-size: var(--text-lg)
   - font-weight: 400
   - color: var(--muted)
   - margin-top: var(--space-sm)
   - line-height: 1.4

5. HERO CONTENT:
   - h1 text: "John Litzsinger"
   - p text: "Finance @ Purdue, interested in startups."

6. TOP BAR: In BaseLayout.astro, the .brand-name span currently shows on homepage. Hide it specifically when on the homepage (no breadcrumbs = homepage). Add a `homepage` boolean prop to BaseLayout, OR detect via `!breadcrumbs` condition. Simpler: add `hideNavName` prop (boolean, default false). When true, add CSS class `brand-name-hidden` to the .brand element. In BaseLayout <style>, add `.brand-name-hidden .brand-name { visibility: hidden; }` — keep layout stable but hide text. Pass `hideNavName` from index.astro.

   ALTERNATIVELY (simpler): Just leave the brand name in the top bar — it's small and won't compete with the large hero name visually. Only remove if it feels cluttered after viewing. Document that the name is also in the top bar as a secondary reference. Use discretion here — the user said "brand name moves to hero position" which implies it should be the dominant instance, not that the top bar must be empty.

   DECISION: Hide top-bar brand name on homepage. Add `hideNavName={true}` prop to BaseLayout. In BaseLayout, conditionally apply `style="visibility: hidden"` to .brand-name span when hideNavName=true. This preserves layout width (no shift) but hides text.

7. LOGO SIZE: In BaseLayout.astro, increase brand-icon size from 28px to 36px. Update the `<img width="28" height="28">` to `width="36" height="36"` and update the .brand-icon CSS width/height. Also update the `.brand` gap from var(--space-sm) to something that still works at 36px.

8. .folder-grid: Remove position absolute. Use normal flow. margin-top: var(--space-xl). Keep justify-content: center, flex-wrap: wrap, gap: var(--space-xl).

9. Remove --chrome-above custom property (no longer needed with flow layout).

10. Verify .home still centers name+folders as a group: .home should be `display: flex; flex-direction: column; align-items: center; justify-content: center; flex: 1;`.

DO NOT add project thumbnail images (deferred). DO NOT add dark mode.
  </action>
  <verify>
    <automated>cd /home/johnl/Desktop/johnlitzsingercom && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
- npm run build passes with no errors
- Homepage shows "John Litzsinger" as large hero name (Urbanist, ~3-4rem)
- Tagline "Finance @ Purdue, interested in startups." appears below in muted color
- jl logo is 36px in top-left
- Brand name in top bar is hidden (visibility: hidden) on homepage
- Folder grid appears below name+tagline, all centered as a group in the viewport
- No absolute positioning in index.astro (flow-based layout)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify homepage hero layout</name>
  <what-built>Homepage hero layout — brand name as large heading above folders, tagline as subtitle, updated logo size</what-built>
  <action>Pause for human visual verification of the homepage hero layout built in Task 1.</action>
  <how-to-verify>
1. Run: npm run dev (localhost:4321)
2. Visit http://localhost:4321
3. Verify: "John Litzsinger" appears large and prominent above the folder grid (Urbanist font, bold, ~3-4rem)
4. Verify: "Finance @ Purdue, interested in startups." appears below the name in muted color
5. Verify: jl logo in top-left is slightly larger than before
6. Verify: Top bar does NOT show "John Litzsinger" text (only logo + social icons)
7. Verify: Folder grid (Now, Work, Guest Book, About) appears below the name, centered
8. Verify: Name + folders group feels visually centered in the viewport (not pushed to top or bottom)
9. Resize to 320px width — ensure name and tagline wrap cleanly, folders still centered
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues to address</resume-signal>
</task>

</tasks>

<verification>
- npm run build passes with zero errors
- Homepage layout matches locked design: hero name + tagline + folder grid centered as group
- No regressions on other pages (top bar logo size update is global)
</verification>

<success_criteria>
- "John Litzsinger" is the dominant typographic element on the homepage
- Tagline reads clearly as secondary information below
- jl logo is 36px in top bar
- Folder grid remains the interactive centerpiece
- Layout is flow-based (no absolute positioning), naturally responsive
</success_criteria>

<output>
After completion, create `.planning/phases/01-design-polish/01-01-SUMMARY.md`
</output>
