---
phase: 01-design-polish
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/now.astro
  - src/pages/about.astro
  - src/pages/404.astro
  - src/layouts/BlogPostLayout.astro
  - src/data/status.ts
autonomous: false
requirements:
  - DESIGN-01
  - DESIGN-03
  - DESIGN-05
  - DESIGN-06
must_haves:
  truths:
    - "Now page shows a clean list of post titles and dates (no cards, no excerpts, no descriptions)"
    - "About page shows bio text above, then resume link, then Calvin & Hobbes GIF below — sequential top-to-bottom"
    - "404 page uses the folder metaphor ('This folder is empty') and links home"
    - "Blog post pages show polished prose with tag pills styled in section color and metadata clearly secondary"
    - "Footer status reads as 'Currently building: [link] · open to startup opportunities'"
  artifacts:
    - path: "src/pages/now.astro"
      provides: "Ultra-minimal table-of-contents layout"
      contains: "entry-list"
    - path: "src/pages/about.astro"
      provides: "Sequential bio layout"
      contains: "about-page"
    - path: "src/pages/404.astro"
      provides: "Folder metaphor 404"
      contains: "not-found"
    - path: "src/layouts/BlogPostLayout.astro"
      provides: "Polished blog post layout"
      contains: "prose"
    - path: "src/data/status.ts"
      provides: "Updated footer status"
      contains: "currentStatus"
  key_links:
    - from: "src/pages/now.astro"
      to: "/blog/[slug]"
      via: "anchor href on each row"
      pattern: "/blog/"
    - from: "src/pages/404.astro"
      to: "/"
      via: "anchor href go home link"
      pattern: "href=\"/\""
---

<objective>
Polish inner pages: redesign the Now page to ultra-minimal table-of-contents, polish the About page sequential layout, transform the 404 page with the folder metaphor, and audit/polish the Blog post layout. Also update footer status copy.

Purpose: Each page needs distinct polish aligned with the "sharp craft" philosophy. No cards on the Now page — just titles and dates. The 404 must use the folder metaphor to stay on-brand.
Output: Polished now.astro, about.astro, 404.astro, BlogPostLayout.astro, status.ts
</objective>

<execution_context>
@/home/johnl/.claude/get-shit-done/workflows/execute-plan.md
@/home/johnl/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-design-polish/01-CONTEXT.md
@.planning/phases/01-design-polish/01-RESEARCH.md

<interfaces>
<!-- From src/layouts/BaseLayout.astro -->
Props: title, description, scrollable?, breadcrumbs?
Section colors set via --section-color CSS variable on <html>
-- now-500: oklch(0.70 0.17 264)  (blue)
-- about-500: oklch(0.70 0.17 295)  (purple)

<!-- From src/styles/global.css -->
--text-xs: 0.8125rem
--text-sm: 0.875rem
--text-base: 1rem
--text-lg: 1.125rem
--text-xl: 1.375rem
--text-2xl: clamp(1.75rem, 1.46rem + 1.15vw, 2.5rem)
--space-xs: 0.25rem, --space-sm: 0.5rem, --space-3: 0.75rem
--space-md: 1rem, --space-6: 1.5rem, --space-lg: 2rem, --space-xl: 4rem
--font-mono: system monospace stack
--muted: #666666
--secondary: rgba(10,10,10,0.60)
--border: oklch(0.10 0.00 0 / 0.12)

<!-- From src/data/status.ts -->
export const currentStatus = { text: 'building this website', url: '/work/personal-website' };
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Now page — ultra-minimal table-of-contents</name>
  <files>src/pages/now.astro</files>
  <action>
Redesign now.astro to be an ultra-minimal list of blog post titles and dates. NO cards, NO excerpts, NO descriptions, NO post-dot indicators.

NEW LAYOUT:
- .now-page: flex column, align-items center, flex: 1
- Keep the "Last updated" time element at the top (monospace, muted, small)
- Replace .entry-feed / .entry-card blocks with a simple .entry-list

For blog posts: render as a `<ul>` (class="entry-list") containing `<li>` items, each being an `<a>` link with:
  - LEFT: post title (font-weight: 500, text-base, color: var(--foreground))
  - RIGHT: date formatted as "Feb 14" or "Mar 9" (short month + day only, no year unless different year) — font-family: var(--font-mono), font-size: var(--text-xs), color: var(--muted)
  - Layout: flex row, justify-content: space-between, align-items: baseline
  - Hover: title color shifts to var(--now-500), no transform/shadow
  - Bottom border: 1px solid var(--border-subtle) on each row EXCEPT the last
  - Transition: color var(--duration-fast) ease

For status updates: render them as a subtle note below the post list (or remove them entirely from the list view since Now page is "table of contents"). Decision: OMIT status updates from the entry list — show ONLY blog posts in the table of contents. If there are no posts, show empty state ("No entries yet.").

.entry-list STYLES:
  - list-style: none
  - width: 100%
  - max-width: 480px
  - margin-top: var(--space-md)

.entry-row (each li > a):
  - display: flex
  - justify-content: space-between
  - align-items: baseline
  - gap: var(--space-lg)
  - padding: var(--space-3) 0
  - text-decoration: none
  - color: inherit
  - border-bottom: 1px solid var(--border-subtle)
  - transition: color var(--duration-fast) ease

.entry-row:last-child: border-bottom: none

.entry-row:hover .entry-title: color: var(--now-500)

.entry-title: flex: 1, font-size: var(--text-base), font-weight: 500, color: var(--foreground)
.entry-date: font-family: var(--font-mono), font-size: var(--text-xs), color: var(--muted), white-space: nowrap, flex-shrink: 0

Date format: use toLocaleDateString with { month: 'short', day: 'numeric' } — do NOT include year.

Remove ALL card styling (box-shadow, background, border-radius on cards, post-dot, update cards).
Keep scrollable={false} (default) — Now page should fit in viewport on desktop.
  </action>
  <verify>
    <automated>cd /home/johnl/Desktop/johnlitzsingercom && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
- Build passes with no errors
- Now page shows a plain list of post titles + dates (no cards, no excerpts)
- Each row: title left, date right, bottom border separator
- Hover state: title turns blue (now-500)
- "Last updated" timestamp visible at top
- Empty state message if no posts
  </done>
</task>

<task type="auto">
  <name>Task 2: About, 404, Blog layout polish + status copy</name>
  <files>src/pages/about.astro, src/pages/404.astro, src/layouts/BlogPostLayout.astro, src/data/status.ts</files>
  <action>
Four targeted polish changes:

--- src/pages/about.astro ---
The current layout is already close to correct (bio → resume → GIF). Polish:
1. Bio text: Rewrite to be personality-first per locked decision. New text:
   "I'm John — I care about markets, startups, and what happens when technology meets money. Currently a finance student at Purdue, co-founder of Launch Consulting Club, and always building something."
2. Resume link: Add subtle section-color hover. Change .resume-link color on hover to var(--about-500). Add transition.
3. about-gif: max-width stays 280px. Add box-shadow: var(--shadow-sm) — subtle depth.
4. about-caption: Keep as-is (italic monospace "taking it easy").
5. .about-body gap: reduce from var(--space-lg) to var(--space-md) — currently slightly too spaced.
6. Add scrollable={true} to BaseLayout props — about page has GIF and may overflow on short viewports.

--- src/pages/404.astro ---
Transform into folder-metaphor 404:
1. Replace h1 "404" with folder SVG icon (same SVG pattern as homepage folders) at ~72px, using var(--accent) color for --folder-color
2. Add h1 below the icon: "This folder is empty" — font-size: var(--text-xl), font-weight: 600, letter-spacing: -0.02em, color: var(--foreground)
3. Replace p text with: "The page you're looking for doesn't exist." — keep as-is (already correct)
4. Style the "Go home" anchor as a proper link: color: var(--accent), text-decoration: underline, text-underline-offset: 3px
5. Remove the old h1 CSS that referenced font-size: clamp(3rem...) — no longer needed
6. The 404 page breadcrumb currently passes label: '404' which doesn't map to any section color (stays red). Keep this — red accent is appropriate for error state.
7. .not-found: max-width: 400px, text-align: center, gap: var(--space-md) between elements

The folder SVG to use (copy from index.astro, single folder, accent color):
```
<svg viewBox="0 0 80 64" width="72" height="58" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
  <rect x="0" y="14" width="80" height="50" rx="4" fill="var(--accent)" />
  <path d="M0 6a4 4 0 0 1 4-4h22a4 4 0 0 1 3.2 1.6L34 10H0V6Z" fill="var(--accent)" />
  <rect x="0" y="10" width="80" height="4" fill="var(--accent)" opacity="0.85" />
</svg>
```

--- src/layouts/BlogPostLayout.astro ---
Minor polish only (layout is already solid):
1. Prose blockquote: change border-left color from var(--accent) to var(--section-color, var(--accent)) — makes blockquote accent match current section color (blog is under "Now" = blue)
2. .post-nav-link:hover color: change from var(--accent) to var(--section-color, var(--accent))
3. .post-nav-arrow color: change from var(--accent) to var(--section-color, var(--accent))
4. Add scrollable={true} is already handled at the page level (blog/[...slug].astro) — do NOT modify BlogPostLayout.astro for this
5. Tag pills: Already use var(--section-color, var(--accent)) — correct. No change needed.
6. h1 in .post-header: change color from implicit (inherits foreground) to explicitly var(--foreground) — just for clarity

--- src/data/status.ts ---
Update footer status to reflect "open to opportunities" message per locked decision:
```ts
export const currentStatus = {
  text: 'building johnlitzsinger.com · open to startup opportunities',
  url: null,
};
```
Note: The BaseLayout footer splits text on first space to get "building:" prefix vs rest. This pattern won't work well for the new text. Instead, update status.ts to export a structured object with separate fields, AND update BaseLayout.astro footer to use them:

New status.ts:
```ts
export const currentStatus = {
  label: 'building',
  project: 'johnlitzsinger.com',
  projectUrl: '/work/personal-website',
  note: 'open to startup opportunities',
};
```

Update BaseLayout.astro footer span to render:
"Currently building: [projectUrl link] · [note]"

Specifically, update the footer JSX to:
```astro
<span class="footer-status">
  <span class="footer-dot" aria-hidden="true"></span>
  Currently {currentStatus.label}:{' '}
  {currentStatus.projectUrl ? (
    <a href={currentStatus.projectUrl} class="footer-status-link">{currentStatus.project}</a>
  ) : (
    <span>{currentStatus.project}</span>
  )}
  {currentStatus.note && (
    <span class="footer-note"> · {currentStatus.note}</span>
  )}
</span>
```

Add .footer-note CSS: color: var(--muted), font-family: var(--font-mono).

IMPORTANT: When updating BaseLayout.astro for the footer change, also update the import statement at the top if status.ts fields change.
  </action>
  <verify>
    <automated>cd /home/johnl/Desktop/johnlitzsingercom && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
- Build passes with no errors
- About page shows rewritten personality-first bio, GIF below, with subtle shadow
- 404 page shows folder SVG icon + "This folder is empty" heading + "Go home" link
- Blog post blockquotes and nav arrows use section color (blue when in Now section)
- Footer reads: "Currently building: johnlitzsinger.com · open to startup opportunities"
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify inner page polish</name>
  <what-built>Inner page polish — Now table-of-contents, About bio rewrite, 404 folder metaphor, Blog prose polish, footer status update</what-built>
  <action>Pause for human visual verification of all inner page polish changes built in Tasks 1 and 2.</action>
  <how-to-verify>
1. Run: npm run dev (localhost:4321)
2. Visit http://localhost:4321/now
   - Should show a minimal list of post titles (left) and short dates (right)
   - No cards, no excerpts — just rows with bottom borders
   - Hover on a row: title turns blue
3. Visit http://localhost:4321/about
   - Bio reads personality-first (interests, values, then professional)
   - GIF visible below bio with subtle shadow
   - Caption "taking it easy" visible in italic monospace
4. Visit http://localhost:4321/404 (navigate to a non-existent URL)
   - Folder icon (red/accent colored) visible at top
   - Heading: "This folder is empty"
   - "Go home" link present and styled
5. Visit any blog post at http://localhost:4321/blog/[slug]
   - Blockquotes should have blue left border (section-color)
   - Post nav arrows should be blue
   - Tag pills look clean and styled
6. Check footer on any page: "Currently building: [link] · open to startup opportunities"
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues to address</resume-signal>
</task>

</tasks>

<verification>
- npm run build passes with zero errors after all changes
- Now page is ultra-minimal table-of-contents style
- About page bio is personality-first
- 404 page uses folder metaphor
- Blog post chrome uses section colors
- Footer has updated status copy
</verification>

<success_criteria>
- DESIGN-01: Now page has minimal title+date list layout, clean hierarchy
- DESIGN-03: About page has bio above, GIF below, sequential flow
- DESIGN-05: Blog post prose, tags, metadata all polished and section-color aware
- DESIGN-06: 404 uses folder metaphor, branded, navigable
- Footer: updated copy with project link + open-to-opportunities note
</success_criteria>

<output>
After completion, create `.planning/phases/01-design-polish/01-02-SUMMARY.md`
</output>
