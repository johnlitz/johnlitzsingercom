---
phase: 01-design-polish
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/layouts/BaseLayout.astro
  - src/pages/work.astro
  - src/components/react/GuestBookPage.tsx
  - docs/design-philosophy.md
autonomous: false
requirements:
  - DESIGN-02
  - DESIGN-04
  - CHROME-01
  - CHROME-02
  - CHROME-03
  - CHROME-04
  - CHROME-05
  - CHROME-06
must_haves:
  truths:
    - "Top bar shows only jl logo and social icons — brand name hidden on homepage, visible on inner pages"
    - "Breadcrumb on inner pages shows section name as title (e.g., 'Work'), not 'jl / Work'"
    - "Back arrow in section-header and breadcrumb is absolutely positioned left, never shifts"
    - "Footer link hover animates underline from left-to-right in section color"
    - "Page transitions between sections happen in 100-150ms ease-in-out"
    - "Section color is consistent: logo hue-rotate, social icon hovers, background tint, underline all match"
    - "Work carousel title/description are clearly above the visual cards"
    - "Guest Book sticky note form has clear alignment, 44px+ touch targets, readable inputs"
    - "Design philosophy markdown document exists at docs/design-philosophy.md"
  artifacts:
    - path: "src/layouts/BaseLayout.astro"
      provides: "Chrome foundation: top bar, breadcrumb, footer, transitions, section colors"
      contains: "section-header"
    - path: "src/pages/work.astro"
      provides: "Work page carousel layout"
      contains: "work-page"
    - path: "src/components/react/GuestBookPage.tsx"
      provides: "Guest Book form alignment"
      contains: "GuestBookPage"
    - path: "docs/design-philosophy.md"
      provides: "Portable design philosophy document"
      contains: "Universal Principles"
  key_links:
    - from: "src/layouts/BaseLayout.astro"
      to: "src/styles/global.css"
      via: "--section-color, --icon-filter, --page-bg CSS custom properties"
      pattern: "var(--section-color)"
    - from: "src/pages/work.astro"
      to: "src/components/react/ProjectCarousel.tsx"
      via: "client:load React island"
      pattern: "ProjectCarousel"
---

<objective>
Polish chrome components (top bar, breadcrumb-as-title, footer, section transitions, section color system) and inner page layouts for Work and Guest Book. Also create the design philosophy document.

Purpose: Chrome is the frame of every page. Correct transitions, consistent section colors, and a clean back-arrow system make the site feel intentional. Work and Guest Book need spacing polish.
Output: Polished BaseLayout.astro (chrome), work.astro, GuestBookPage.tsx spacing, docs/design-philosophy.md
</objective>

<execution_context>
@/home/johnl/.claude/get-shit-done/workflows/execute-plan.md
@/home/johnl/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-design-polish/01-CONTEXT.md
@.planning/phases/01-design-polish/01-RESEARCH.md

<interfaces>
<!-- From src/layouts/BaseLayout.astro (current state) -->
- .section-header: flex row, centered, gap var(--space-sm), position relative
- .section-back: position absolute, left: var(--gutter), 36x36px, border-radius 8px
- .section-title: font-size var(--text-2xl), color: var(--section-color), font-weight 600
- .breadcrumb: flex row, centered, gap var(--space-sm), position relative
- .back-arrow: position absolute, left: var(--gutter), 32x32px
- ContentFade: fade({ duration: '0.15s' }) — currently 150ms, within the 100-150ms target
- html transition: background-color var(--duration-slow) ease — 300ms (needs to be 150ms for section color shift)
- sectionColorMap: Now/Work/Guest Book/About → OKLCH colors + bg tints
- sectionFilterMap: hue-rotate filters per section for logo icon

<!-- From src/styles/global.css -->
--duration-fast: 150ms
--duration-base: 200ms
--duration-slow: 300ms

<!-- From src/components/react/ProjectCarousel.tsx -->
(will need to read during task — key: carousel-info div has title/description, check positioning relative to cards)

<!-- From src/components/react/GuestBookPage.tsx -->
(will need to read during task — key: form input heights, sticky note layout)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: BaseLayout chrome audit and polish</name>
  <files>src/layouts/BaseLayout.astro</files>
  <action>
Audit and polish BaseLayout.astro chrome components:

1. SECTION TRANSITION SPEED:
   - html background-color transition is currently `var(--duration-slow)` (300ms). Change to `var(--duration-fast)` (150ms) for section color shifts. This matches the locked decision: "100-150ms ease-in-out."
   - Keep `ease` (not ease-in-out) since CSS `ease` is a reasonable default. Actually: use `ease-in-out` per the locked decision. Update: `transition: background-color var(--duration-fast) ease-in-out;`
   - Also update .brand-icon filter transition to var(--duration-fast) ease-in-out (currently --duration-slow).

2. TOP BAR BRAND NAME (homepage handling — coordinate with Plan 01):
   - If Plan 01 adds hideNavName prop, honor it. If not yet added, add the prop to the interface and handling here.
   - Props interface: add `hideNavName?: boolean`
   - In the .brand-name span: conditionally add class `hidden-on-home` when hideNavName=true
   - CSS: `.hidden-on-home { visibility: hidden; }` — preserves layout space

3. SECTION-HEADER AUDIT (breadcrumb as title):
   - Current: shows folder icon + section title in section color. This IS the breadcrumb-as-title pattern.
   - Verify: No "jl /" prefix in section-header (there isn't currently — confirm and document)
   - The normal breadcrumb path (for blog posts) still shows "jl › Now › Post Title" — this is fine for multi-level
   - section-back: already position absolute left var(--gutter) — CORRECT, verify it doesn't shift

4. FOOTER STATUS LINK ANIMATION:
   - Current .footer-status-link::after uses `right: 100%` to start and `right: 0` on hover — this is LEFT-to-RIGHT slide.
   - Verify: `left: 0; right: 100%` (starts as invisible, full left). On hover: `left: 0; right: 0` — CORRECT.
   - Also verify the underline color uses `var(--section-color, var(--accent))` — already does.
   - Transition: `right 0.25s cubic-bezier(0.22, 1, 0.36, 1)` — this is fine, snappy spring.

5. SOCIAL LINKS HOVER:
   - Verify hover uses `var(--section-color, var(--accent))` — already does.
   - No changes needed.

6. SECTION COLOR SYSTEM VERIFICATION:
   - sectionColorMap keys: 'Now', 'Work', 'Guest Book', 'About' — all present.
   - sectionFilterMap keys: same — all present.
   - homepage (no breadcrumbs): sectionColor = var(--accent) (red), iconFilter = 'none'. Correct.
   - Add missing icon filter for homepage: when no section, icon should show its natural red/brand color (filter: none is correct).

7. SKIP NAV LINK:
   - Currently exists but has no CSS for display. Add minimal CSS:
     `.skip-nav { position: absolute; transform: translateY(-100%); left: var(--gutter); top: 0; padding: var(--space-sm) var(--space-md); background: var(--foreground); color: var(--background); font-size: var(--text-sm); border-radius: 0 0 8px 8px; transition: transform var(--duration-fast) ease; z-index: 100; }`
     `.skip-nav:focus { transform: translateY(0); }`

8. TRANSITION ANIMATION EASING:
   - Brand name underline uses `cubic-bezier(0.22, 1, 0.36, 1)` (spring) — keep.
   - Footer link underline uses same — keep.
   - Main content fade: 150ms — already correct.

Changes summary: Update html background-color transition to 150ms ease-in-out, update brand-icon filter transition to 150ms ease-in-out, add hideNavName prop + CSS, add skip-nav CSS. All other chrome is already correct per audit.
  </action>
  <verify>
    <automated>cd /home/johnl/Desktop/johnlitzsingercom && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
- Build passes
- html background-color transition is 150ms ease-in-out (fast, snappy section switches)
- Brand icon filter transitions at 150ms
- hideNavName prop accepted and causes .brand-name to hide visually (visibility: hidden)
- Skip-nav link is accessible on focus
- No regressions on existing section-header, breadcrumb, footer animations
  </done>
</task>

<task type="auto">
  <name>Task 2: Work page and Guest Book spacing polish + design philosophy doc</name>
  <files>src/pages/work.astro, src/components/react/GuestBookPage.tsx, docs/design-philosophy.md</files>
  <action>
Three items:

--- src/pages/work.astro + ProjectCarousel.tsx ---
Read src/components/react/ProjectCarousel.tsx first to understand current layout.

The work page just renders `<ProjectCarousel client:load projects={projects} />`. The carousel component itself controls all layout. The locked decision says "title/description above visual cards."

In ProjectCarousel.tsx:
- Find the carousel-info div (or equivalent) with title and description
- Verify it renders ABOVE the card visual area (not below, not overlapping)
- If title/description are already above cards: just verify spacing is correct (no changes needed to structure)
- Add padding/margin polish if needed: title should have font-weight: 600, font-size: var(--text-xl) or larger. Description should be var(--text-sm), color: secondary/muted.
- Tech pills: should be compact, monospace, muted. Each pill: font-family monospace, font-size var(--text-xs), background: color-mix(in srgb, var(--section-color) 8%, transparent), padding: 2px 8px, border-radius: 4px
- Note: ProjectCarousel is a React component — use React inline style objects OR Tailwind-equivalent (none — use inline style with CSS custom properties since no Tailwind in this project)
- Carousel cards: ensure they have colored shadow (not black). Shadow: `drop-shadow or box-shadow with color-mix(in srgb, var(--work-500) 20%, transparent)`. NOTE: React component can't access CSS custom properties via `style` prop directly, but CAN via CSS variables if they cascade. The --work-500 is defined globally, so `color-mix(in srgb, var(--work-500) 20%, transparent)` in inline style will work.

--- src/components/react/GuestBookPage.tsx ---
Read the file first. Polish for alignment and spacing:
- Form inputs: min-height: 44px (touch-friendly)
- Textarea: min-height: 100px
- Submit button: min-height: 44px, cursor: pointer
- Signature canvas: ensure it has a visible border/outline (border: 1px solid var(--border))
- Sticky note entries: if padding < 16px, increase to 16px (var(--space-md))
- Overall form container: max-width should not exceed 540px, centered
- Note: GuestBookPage.tsx uses inline styles (React component). Adjust inline style values. Use CSS custom properties where possible (they cascade into React inline styles).

--- docs/design-philosophy.md ---
Create a two-layer design philosophy document. Ensure the `docs/` directory exists first.

Content structure:
```
# Design Philosophy

## Universal Principles

### Spacing: Every Element Earns Its Place
...

### Typography: Hierarchy Without Decoration
...

### Color: Context-Dependent Life
...

### Motion: Statement Moments Only
...

### Craft Standard: Pixel-Perfect Obsession
...

## Site-Specific Decisions

### Palette: Warm Cream Light-Only
...

### Type System: Rethink Sans + Urbanist
...

### Section Identity: The Folder Aesthetic
...

### OKLCH Color Scales
...

### Layout: Viewport-Locked Column
...
```

Keep it ~500 words total. Write in first-person declarative style ("I use X because Y"). This is a personal philosophy document, not corporate documentation. Be direct and opinionated. Reference the Apple iPod aesthetic: "bold minimal silhouettes with color bursts."
  </action>
  <verify>
    <automated>cd /home/johnl/Desktop/johnlitzsingercom && npm run build 2>&1 | tail -5 && ls docs/design-philosophy.md</automated>
  </verify>
  <done>
- Build passes
- Work page carousel shows title/description clearly above card area
- Tech pills are compact, monospace, section-color tinted
- Guest Book form inputs are 44px+ height
- Signature canvas has visible border
- docs/design-philosophy.md exists with two-layer structure (universal + site-specific)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify chrome polish and design doc</name>
  <what-built>Chrome polish (transition speed, section colors, skip-nav), Work carousel polish, Guest Book form spacing, design philosophy doc</what-built>
  <action>Pause for human visual verification of chrome polish and design philosophy document created in Tasks 1 and 2.</action>
  <how-to-verify>
1. Run: npm run dev (localhost:4321)
2. Navigate Now → Work → About → Guest Book and back. Check:
   - Background color transitions happen FAST (~150ms), not slow
   - jl logo color shifts immediately with each section
   - Social icon hovers change to each section's color
3. Visit http://localhost:4321/work
   - Carousel title and description are ABOVE the visual cards
   - Tech pills are small, compact, section-color tinted (green for Work)
4. Visit http://localhost:4321/guest-book
   - Form inputs are reasonably tall (44px+)
   - Signature canvas has a visible border
   - Layout is centered and well-spaced
5. Visit http://localhost:4321/about
   - Press Tab to check skip-nav link appears on focus (keyboard accessibility)
6. Open docs/design-philosophy.md and verify it has content in both sections (Universal Principles + Site-Specific Decisions)
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues to address</resume-signal>
</task>

</tasks>

<verification>
- npm run build passes with zero errors
- Section transitions are 150ms (fast, not 300ms)
- All section color elements (logo, hovers, backgrounds, underlines) consistent
- Work page has properly positioned carousel elements
- Guest Book form is touch-friendly
- Design philosophy doc exists
</verification>

<success_criteria>
- CHROME-01: Top bar clean, logo larger, brand name hidden on homepage
- CHROME-02/03: Section breadcrumb-as-title works, back arrow stays left
- CHROME-04: Footer link hover animates in section color
- CHROME-05: Page transitions are 150ms ease-in-out (snappy)
- CHROME-06: Section color system is consistent across logo, hovers, backgrounds, underlines
- DESIGN-02: Work carousel title/description above cards
- DESIGN-04: Guest Book form has proper spacing and touch targets
</success_criteria>

<output>
After completion, create `.planning/phases/01-design-polish/01-03-SUMMARY.md`
</output>
